---
# Home Assistant Cloud
# https://www.home-assistant.io/cloud/

cloud:
  alexa:
    filter:
      include_entities: switch.shack_external_lights
      include_domains: switch
    entity_config:
      switch.shack_external_lights:
        name: Shack External Lights
        description: The lights outside the shack
